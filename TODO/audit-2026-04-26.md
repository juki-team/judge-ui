# Auditoría judge-ui — 2026-04-26

Hallazgos del análisis del proyecto. Ordenado por prioridad de impacto.

---

## 🐛 Bugs / errores concretos

- [ ] **`src/app/(user)/RootLayout.tsx:40`** — Quitar `console.log('RootLayout', pathname)` dejado en producción.
- [ ] **`src/app/(user)/(bare)/print/problem/page.tsx:30,33`** — Reemplazar `console.error()` por un error boundary real.
- [ ] **`src/app/(user)/(bare)/print/contest/problem-set/page.tsx`** — Revisar uso de `<Fragment key={...}>`; preferir un wrapper concreto.
- [ ] **`next.config.mjs`** — `reactStrictMode: false` desactiva las verificaciones de doble render de React 19. Reactivar y corregir lo que rompa.
- [ ] **TODOs sin contexto** — Limpiar/documentar TODOs pendientes en:
  - `ProblemSettings.tsx`
  - `CrawlCodeforcesProblemModal.tsx`
  - (y 3+ más)

---

## ⚡ Carga / performance

### Estado actual
- Solo **2 archivos `loading.tsx`** en toda la app (`src/app/loading.tsx`).
- **1 solo `error.tsx`** a nivel raíz.
- **0 usos de `<Suspense>`** en el código.
- **~80 archivos con `'use client'`** — la app es casi 100% client-side.
- **45+ `useFetcher` (SWR)** que muestran `isLoading` pero sin skeleton; queda vacío o con datos viejos.
- Solo **3 `dynamic()` imports** (`Analytics`, `JukiI18nInitializer`, `StylesLazy`).

### Dependencias pesadas cargadas eager
- [ ] **`@excalidraw/excalidraw` (~1MB)** — usado en **un solo archivo** (`ProblemStatistics.tsx`). Convertir a `dynamic(() => import(...), { ssr: false })`.
- [ ] **`recharts`** — usado en scoreboard. Code-split por tab.
- [ ] **`ably` + `@ably/spaces`** — WebSockets cargados siempre, incluso en rutas que no los usan. Cargar bajo demanda.
- [ ] **`@ai-sdk/react`** — cargado a nivel app. Mover a las rutas que realmente lo usan.

### Falta `loading.tsx` en rutas críticas
- [ ] `src/app/(user)/(main)/contests/[contestKey]/loading.tsx`
- [ ] `src/app/(user)/(main)/problems/[problemKey]/loading.tsx`
- [ ] `src/app/(user)/(main)/profiles/[userKey]/loading.tsx`
- [ ] `src/app/(user)/(main)/ranking/loading.tsx`

---

## 🎨 Skeleton loaders — plan recomendado

`@juki-team/base-ui` ya expone `SpinIcon` y `LoaderLayer`, pero **no hay primitiva `Skeleton`**.

### 1. Crear primitivas genéricas en `src/components/skeletons/`
- [ ] `Skeleton.tsx` — bloque base con animación shimmer (CSS `@keyframes`).
- [ ] `SkeletonText.tsx` — líneas de texto con anchos aleatorios.
- [ ] `SkeletonCard.tsx` — card con avatar + título + 2 líneas.
- [ ] `SkeletonRow.tsx` — fila para `DataViewer` (compatible con columnas existentes).
- [ ] `SkeletonScoreboard.tsx` — específico para tabla de ranking/scoreboard.

### 2. Integrar
- [ ] Agregar `loading.tsx` en cada ruta anidada citada arriba, devolviendo el skeleton apropiado.
- [ ] Envolver fetches paralelos en `<Suspense>` dentro de `ViewContest.tsx` para que cada tab (overview, scoreboard, events, clarifications) muestre skeleton independiente sin bloquear los demás.
- [ ] Reemplazar `useContestData.tsx:95` (`isLoadingAny: l1 || l2 || l3 || l4`) por skeletons por sección. Hoy un fetch lento bloquea visualmente todo.
- [ ] Mover el shimmer a CSS puro (sin JS) en `src/components/styles/` para que funcione incluso antes de hidratar.

---

## 🔍 SEO / a11y

- [ ] **`src/app/layout.tsx`** — No exporta `viewport`. Next 16 lo recomienda separado de `metadata`.
- [ ] **Soporte de tema** — `<body className="jk-theme-light">` está hardcodeado. Agregar soporte para `prefers-color-scheme`.
- [ ] **`theme-color` meta tag** — Falta.
- [ ] **Cobertura de `alt`** — Solo **12 atributos `alt=`** en todo `src/components/`. Auditar imágenes sin texto alternativo.
- [ ] **`generateMetadata()` dinámica** faltante en:
  - `problems/[problemKey]/page.tsx`
  - `profiles/[userKey]/page.tsx`
  - `ranking/page.tsx`
  - (`contests/[contestKey]/page.tsx:15-44` ya lo hace ✓)

---

## 🛡️ Resiliencia

- [ ] **Falta `error.tsx` en rutas anidadas.** Una falla en `/contests/X` hoy sube hasta el root y reemplaza toda la UI. Agregar:
  - [ ] `src/app/(user)/(main)/error.tsx`
  - [ ] `src/app/(user)/(main)/contests/[contestKey]/error.tsx`
  - [ ] `src/app/(user)/(main)/problems/[problemKey]/error.tsx`
  - [ ] `src/app/(user)/(main)/profiles/[userKey]/error.tsx`
- Nota: `RootLayout.tsx:71` (`NODE_ENV !== 'production' ? app : <ErrorBoundary>`) es intencional — en dev se quiere ver el error crudo. No tocar.

---

## ✅ Lo que está bien (no tocar)

- `tsconfig.json` con `strict: true`, `noUnusedLocals`, `noUnusedParameters` activos.
- `generateMetadata()` dinámica en página de contest.
- Image optimization configurado en `next.config.mjs` con `remotePatterns`.
- SWR usado consistentemente con `useFetcher`.
- Header `X-Robots-Tag` configurado.

---

## 🎯 Orden de ejecución sugerido

1. [ ] Quitar `console.log` de `RootLayout.tsx:40` (5 segundos).
2. [ ] `dynamic()` para Excalidraw y Recharts (gran ganancia de bundle inicial).
3. [ ] Crear primitivas Skeleton + agregar `loading.tsx` en 4 rutas (mayor impacto UX percibido).
4. [ ] Agregar `error.tsx` en `(user)/(main)/` y rutas dinámicas (resiliencia).
5. [ ] Reactivar `reactStrictMode: true` y arreglar lo que rompa.
6. [ ] Envolver tabs de `ViewContest` en `<Suspense>` (streaming real).
7. [ ] Completar `generateMetadata()` y cobertura de `alt`.